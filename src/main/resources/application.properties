# Datasource
spring.datasource.url = jdbc:postgresql://localhost:5432/finalprojectitacademy
spring.datasource.username = postgres
spring.datasource.password = 16091997
spring.datasource.driverClassName=org.postgresql.Driver
server.port=8130

# Hibernate
spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql = true

spring.queries.users-query=select email, password from users where email=?
spring.queries.roles-query=select u.email, u.role from users u where u.email=?